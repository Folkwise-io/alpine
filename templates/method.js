// Method definition
function {{=it.method.name}}() {
  
}

// Alpine method definition and export
module.exports = {
  name: "{{=it.method.name}}",
  parameters: [],
  value: {{=it.method.name}},
};
