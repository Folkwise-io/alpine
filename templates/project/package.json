{
  "name": "{{=it.project.name}}",
  "version": "0.0.1",
  "description": "{{=it.project.description}}",
  "main": "index.js",
  "author": "{{=it.project.author}}",
  "license": "{{=it.project.license}}",
  "private": "{{=it.project.private}}",
  "scripts": {
    "test": "mocha ./.alpine/test.js"
  },
  "bin": {
    "{{=it.project.name}}": "./.alpine/cli.js"
  },
  "dependencies": {
    "alpine": "^0.0.1",
    "caporal": "^1.1.0",
    "require-all": "^3.0.0",
    "v8n": "^1.2.3"
  },
  "devDependencies": {
    "eslint": "^5.11.1",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "mocha": "^5.2.0"
  }
}
